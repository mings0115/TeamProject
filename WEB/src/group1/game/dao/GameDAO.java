package group1.game.dao;

public class GameDAO {

	/*
	 * Connection con = null; PreparedStatement ps = null; ResultSet rs = null;
	 * 
	 * private static GameDAO instance = null;
	 * 
	 * private GameDAO() {}
	 * 
	 * public static GameDAO getInstance() {
	 * 
	 * if (instance == null) { instance = new GameDAO(); } return instance; }
	 * 
	 * public int getGame(int gameno) {
	 * 
	 * String sql = "select * from GAME where gameno=?"; int GameNUM = 0;
	 * 
	 * try { con=DBManager.getConnection(); ps=con.prepareStatement(sql);
	 * 
	 * ps.setInt(1, gameno);
	 * 
	 * rs=ps.executeQuery();
	 * 
	 * if(rs.next()) { GameNUM = rs.getInt("gameno"); }
	 * 
	 * } catch (Exception e) { System.out.println(e.getMessage()); } finally {
	 * DBManager.close(con, ps, rs); }
	 * 
	 * return GameNUM; }
	 */
}
